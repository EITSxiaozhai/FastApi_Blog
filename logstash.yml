input {
    tcp {
        port => 5000
        codec => json
    }
}

output {
    elasticsearch {
        ssl => true
        ssl_certificate_verification => true
        hosts => "https://39.108.112.103:9200"
        user => "elastic"
        password => "unXrD4WkSy+TuI53JzWw"
        }
}